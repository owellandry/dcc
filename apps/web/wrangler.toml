name = "dcc-web"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = "pnpm build"

[[routes]]
pattern = "dcc.app/*"
zone_name = "dcc.app"

[vars]
NEXT_PUBLIC_API_URL = "https://api.dcc.app"
NEXT_PUBLIC_WS_URL  = "wss://gateway.dcc.app"
